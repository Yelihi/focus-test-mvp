/** Consecutive no-face frames before transitioning to absent (3s at 10 FPS) */
export const ABSENT_FRAME_THRESHOLD = 30;
/** Run object detection every N frames (3 FPS at 10 FPS capture rate) */
export const PHONE_DETECT_INTERVAL = 3;
